import React from 'react';
import { BookUser } from 'lucide-react-native';
import { StubScreen } from '@/screens/shared/StubScreen';

export function StaffDirectoryScreen() {
  return <StubScreen icon={BookUser} title="Directory" description="Staff directory screen — not built in this pass." />;
}
