import React from 'react';
import { LayoutGrid } from 'lucide-react-native';
import { StubScreen } from '@/screens/shared/StubScreen';

export function StaffTodayScreen() {
  return <StubScreen icon={LayoutGrid} title="Today" description="STAFF tab shell only in this pass — nav/auth built, screens are a follow-up." />;
}
