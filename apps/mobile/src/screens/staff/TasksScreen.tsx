import React from 'react';
import { ListChecks } from 'lucide-react-native';
import { StubScreen } from '@/screens/shared/StubScreen';

export function StaffTasksScreen() {
  return <StubScreen icon={ListChecks} title="Tasks" description="No sms_* tasks module exists yet to port a real contract from." />;
}
